import HeroSection from "@/components/HeroSection";
import { MantineProvider, Container, Title, Text, List, ListItem, Button, Group, Center } from "@mantine/core";

export default function Home() {
	return (
		<>
			<HeroSection />
			{/* Add other sections as needed */}
		</>
	);
}
